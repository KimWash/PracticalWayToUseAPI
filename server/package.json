{
  "name": "test-api",
  "version": "1.0.0",
  "description": "mockapi for know what is practical way to use api!",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch src/db.json --port 4000 --host 0.0.0.0"
  },
  "author": "KyoungMin Choi",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}

